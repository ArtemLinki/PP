1 - ~~в "Зарегестрироваться" автовставка почты осущиствляется в номер телевона~~ **ИСПРАВЛЕНО** — добавлены `autoComplete="email"` и `type="tel"` / `autoComplete="tel"`
2 - ~~в карзину добавлять товары незарегистрированный пользователь~~ **ИСПРАВЛЕНО** — проверка авторизации в `ProductCard` и странице товара, уведомление с предложением войти
3 - ~~В регистрации есть возможность выбрать Физлицо и Юрлицо, но от этого выбора ничего не меняется~~ **ИСПРАВЛЕНО** — в личном кабинете отображается блок «Юридическое лицо» для B2B-пользователей
4 - ~~отзывы на карточках товаров не работаю~~ **ИСПРАВЛЕНО** — реализована полная система отзывов (пункт 14)
5 - ~~положить в карзину можно больше чем указано количество доступных товаров~~ **ИСПРАВЛЕНО** — `max` в NumberInput привязан к `stockQty`; backend бросает 400 при превышении
6 - ~~указания ФИО при регистрации невлияет ни на что~~ **ИСПРАВЛЕНО** — кнопка «Сохранить» в профиле вызывает `PATCH /users/me` и обновляет стор
7 - ~~в категориях напротив имени указаны число найденных совпадений, которое не соотвецтвует количеству уникального товара~~ **ИСПРАВЛЕНО** — `_count` фильтрует только `PUBLISHED` товары
8 - ~~кнопка на верхней панеле "поиск" ничего не делает~~ **ИСПРАВЛЕНО** — кнопка-иконка поиска ведёт на `/catalog`
9 - ~~примеры запросов "ПРИМЕРЫ ЗАПРОСОВ ..." удалить~~ **ИСПРАВЛЕНО** — блок чипов удалён с главной страницы и из `AiPromptCard`
10 - ~~ИИ не должен отвечать на вопросы незвязанные с темой электронники и тп~~ **ИСПРАВЛЕНО** — в системный промпт добавлено явное ограничение тематики
11 - ~~Нет возможности удобного перехода в админку для пользователья с Role.ADMIN~~ **ИСПРАВЛЕНО** — в личном кабинете появился блок «Панель администратора» с кнопкой для ADMIN
12 - ~~нужно текст от ИИ выводить отформатированным (как md)~~ **ИСПРАВЛЕНО** — в `AiChatWindow` ответы рендерятся через `react-markdown`
13 - ~~реализовать кнопку заказа (нужно открывать форму для заполнения данных и/или подтягивать их если пользователь уже оформлял заказ ранне)~~ **ИСПРАВЛЕНО** — `CheckoutModal` с формой доставки, предзаполнение из последнего заказа (или из профиля), очищает корзину после оформления
14 - ~~реализовать систему отзывов~~ **ИСПРАВЛЕНО** — полная система: модель `Review` в БД, backend CRUD (`GET/POST/DELETE /reviews`), компонент `ReviewsSection` с формой (звёздный рейтинг + текст), подгрузка на странице товара, удаление своего отзыва
15 - ~~не сохраняется история общения при общении с ассистентом: нужно сохранять, хотя бы в рамках сессии~~ **ИСПРАВЛЕНО** — `useAiStore` использует `persist` с `sessionStorage`; `conversationId` и `messages` живут до закрытия вкладки
16 - ~~нельзя интерактично чистить отзывы клиентов (добавь в карточки отзывов кнопку как будто это сосбственные отзывы для админа)~~ **ИСПРАВЛЕНО** — `canDelete` в `ReviewCard` теперь `true` при `user.role === 'ADMIN'`
17 - ~~проверь админку: нужно добавить фильтры (в том числе по пользователю) и вывод всех заказов, также в целом проверь админку что она выводит актуальные данные (напрмиер, последние заказы)~~ **ИСПРАВЛЕНО** — дашборд переведён на реальный `GET /admin/dashboard`; страница заказов использует `GET /admin/orders` (все заказы), добавлены фильтры по email и статусу, смена статуса сохраняется через `PATCH /admin/orders/:id/status`
18 - ~~добавь пагинация для вывода списка товаров (сейчас они выводятся все подряд а не частично)~~ **ИСПРАВЛЕНО** — в каталоге добавлен компонент `<Pagination>` Mantine; страница сбрасывается при смене фильтров
19 - ~~исправь проблему с перебрасыванием на login при перезагрузке страницы~~ **ИСПРАВЛЕНО** — в `AdminLayout` добавлен `initializing` стейт; на mount вызывается `hydrate()` по сохранённому токену, редирект происходит только после завершения гидрации
20 - ~~исправь форму для добавления категории/бренд (почему-то исчезают данные при потере фокуса на инпуте названия), в общем формы не работают в админке~~ **ИСПРАВЛЕНО** — `onFocus={handleOpen}` удалён из всех модалов; инициализация стейта перенесена в `useEffect([opened, editing])`
21 - ~~нужно предоставить возможность остановить ИИ по кнопке отправки~~ **ИСПРАВЛЕНО** — кнопка «Отправить» заменяется на кнопку «Стоп» во время генерации; запрос отменяется через `AbortController`, оптимистичное сообщение пользователя убирается
22 - ~~нужно запретить ИИ вставлять ссылки на стронние сервисы~~ **ИСПРАВЛЕНО** — в системный промпт добавлено правило «НИКОГДА не вставляй URL-ссылки на сторонние сайты»
23 - ~~добавь возможность загружать фотографии для товаров и отображать их в слайдере где есть большое фото и есть маленькие~~ **ИСПРАВЛЕНО** — в форме товара (админка) добавлен блок «Изображения»: загрузка файлов через `POST /files/upload` (MinIO), предпросмотр с кнопкой удаления; слайдер с превью уже был реализован на странице товара
24 - ~~убери скидки как сущншсть из проекта~~ **ИСПРАВЛЕНО** — `oldPrice` и badge «Скидка %» удалены из `ProductCard` и страницы товара; данные в БД не трогались
25 - ~~нужно на странице заказов добавить пагинацию для таблицы и еще исправить фильтр по email: он не ищет и еще нужно добавить селект с поиском для фильтрации по пользователю~~ **ИСПРАВЛЕНО** — email TextInput заменён на searchable `Select` с пользователями (загружается через `GET /admin/users`), фильтрация по `userId`; добавлена серверная пагинация (`PAGE_SIZE=20`) с компонентом `<Pagination>`