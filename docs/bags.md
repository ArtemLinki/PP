1 - ~~в "Зарегестрироваться" автовставка почты осущиствляется в номер телевона~~ **ИСПРАВЛЕНО** — добавлены `autoComplete="email"` и `type="tel"` / `autoComplete="tel"`
2 - ~~в карзину добавлять товары незарегистрированный пользователь~~ **ИСПРАВЛЕНО** — проверка авторизации в `ProductCard` и странице товара, уведомление с предложением войти
3 - ~~В регистрации есть возможность выбрать Физлицо и Юрлицо, но от этого выбора ничего не меняется~~ **ИСПРАВЛЕНО** — в личном кабинете отображается блок «Юридическое лицо» для B2B-пользователей
4 - ~~отзывы на карточках товаров не работаю~~ **ИСПРАВЛЕНО** — реализована полная система отзывов (пункт 14)
5 - ~~положить в карзину можно больше чем указано количество доступных товаров~~ **ИСПРАВЛЕНО** — `max` в NumberInput привязан к `stockQty`; backend бросает 400 при превышении
6 - ~~указания ФИО при регистрации невлияет ни на что~~ **ИСПРАВЛЕНО** — кнопка «Сохранить» в профиле вызывает `PATCH /users/me` и обновляет стор
7 - ~~в категориях напротив имени указаны число найденных совпадений, которое не соотвецтвует количеству уникального товара~~ **ИСПРАВЛЕНО** — `_count` фильтрует только `PUBLISHED` товары
8 - ~~кнопка на верхней панеле "поиск" ничего не делает~~ **ИСПРАВЛЕНО** — кнопка-иконка поиска ведёт на `/catalog`
9 - ~~примеры запросов "ПРИМЕРЫ ЗАПРОСОВ ..." удалить~~ **ИСПРАВЛЕНО** — блок чипов удалён с главной страницы и из `AiPromptCard`
10 - ~~ИИ не должен отвечать на вопросы незвязанные с темой электронники и тп~~ **ИСПРАВЛЕНО** — в системный промпт добавлено явное ограничение тематики
11 - ~~Нет возможности удобного перехода в админку для пользователья с Role.ADMIN~~ **ИСПРАВЛЕНО** — в личном кабинете появился блок «Панель администратора» с кнопкой для ADMIN
12 - ~~нужно текст от ИИ выводить отформатированным (как md)~~ **ИСПРАВЛЕНО** — в `AiChatWindow` ответы рендерятся через `react-markdown`
13 - ~~реализовать кнопку заказа (нужно открывать форму для заполнения данных и/или подтягивать их если пользователь уже оформлял заказ ранне)~~ **ИСПРАВЛЕНО** — `CheckoutModal` с формой доставки, предзаполнение из последнего заказа (или из профиля), очищает корзину после оформления
14 - ~~реализовать систему отзывов~~ **ИСПРАВЛЕНО** — полная система: модель `Review` в БД, backend CRUD (`GET/POST/DELETE /reviews`), компонент `ReviewsSection` с формой (звёздный рейтинг + текст), подгрузка на странице товара, удаление своего отзыва
